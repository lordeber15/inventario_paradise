[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
cache_dir = /tmp/.pytest_cache
markers =
    slow: loads real ML models or does heavier I/O; excluded by default in CI-style runs
